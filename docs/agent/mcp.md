# MCP And Tooling

- shadcn MCP is enabled at Codex user level via `~/.codex/config.toml`.
- If MCP tools are available in a future session, prefer them for browsing/searching shadcn registry items before adding unfamiliar components or blocks.
